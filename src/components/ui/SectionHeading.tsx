export function SectionHeading({eyebrow,title,description,light=false}:{eyebrow:string;title:string;description?:string;light?:boolean}){return <div className="max-w-3xl"><p className={`eyebrow ${light?'!text-aqua-400':''}`}>{eyebrow}</p><h2 className={`section-title ${light?'!text-white':''}`}>{title}</h2>{description&&<p className={`body-large mt-6 max-w-2xl ${light?'!text-slate-300':''}`}>{description}</p>}</div>}
