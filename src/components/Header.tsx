import {useEffect,useState} from 'react';import {ArrowUpRight,Menu,X} from 'lucide-react';import {AnimatePresence,motion} from 'framer-motion';import {Logo} from './Logo';
const links=[['Início','inicio'],['Quem Somos','quem-somos'],['Serviços','servicos'],['Soluções','solucoes'],['Diferenciais','diferenciais'],['Qualidade','qualidade'],['Contato','contato']];
export function Header(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const fn=()=>setScrolled(scrollY>20);fn();addEventListener('scroll',fn,{passive:true});return()=>removeEventListener('scroll',fn)},[]);useEffect(()=>{document.body.style.overflow=open?'hidden':'';return()=>{document.body.style.overflow=''}},[open]);return <header className={`fixed inset-x-0 top-0 z-50 transition-all ${scrolled?'border-b border-slate-200/70 bg-white/90 shadow-sm backdrop-blur-xl':'bg-transparent'}`}><div className="container-page flex h-20 items-center justify-between"><Logo/><nav className="hidden items-center gap-5 xl:flex" aria-label="Navegação principal">{links.map(([label,id])=><a key={id} className="text-xs font-bold text-slate-600 transition hover:text-aqua-600" href={`#${id}`}>{label}</a>)}</nav><a className="button-primary hidden !min-h-10 !px-5 lg:inline-flex" href="#contato">Solicite uma proposta <ArrowUpRight size={15}/></a><button className="grid h-12 w-12 place-items-center rounded-full border border-slate-200 bg-white lg:hidden" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="mobile-menu" aria-label={open?'Fechar menu':'Abrir menu'}>{open?<X/>:<Menu/>}</button></div><AnimatePresence>{open&&<motion.div id="mobile-menu" className="fixed inset-0 top-20 bg-navy-950 p-6 lg:hidden" initial={{opacity:0,x:'100%'}} animate={{opacity:1,x:0}} exit={{opacity:0,x:'100%'}} transition={{duration:.3}}><nav className="mx-auto flex max-w-lg flex-col" aria-label="Navegação móvel">{links.map(([label,id],i)=><motion.a key={id} href={`#${id}`} onClick={()=>setOpen(false)} className="border-b border-white/10 py-4 text-2xl font-semibold text-white" initial={{opacity:0,x:20}} animate={{opacity:1,x:0}} transition={{delay:i*.04}}>{label}</motion.a>)}<a href="#contato" onClick={()=>setOpen(false)} className="mt-7 inline-flex min-h-14 items-center justify-center gap-2 rounded-full bg-aqua-500 font-bold text-white">Solicite uma proposta <ArrowUpRight size={18}/></a></nav></motion.div>}</AnimatePresence></header>}
